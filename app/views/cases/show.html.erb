<div class="container">
  <!-- <div class="row"> -->

    <div class="dashboard-tabs">
      <div class="fake-container">
        <div class="fake-column-1"></div>
        <div class="fake-column-2">
          <ul class="nav nav-tabs-dashboard" id="myTab" role="tablist" style="padding-left: 20px;">
            <li class="nav-item active">
              <a class="nav-link" id="overview-tab" data-toggle="tab" href="#overview" role="tab" aria-controls="overview" aria-selected="true"><b>OVERVIEW</b></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" id="messages-tab" data-toggle="tab" href="#messages" role="tab" aria-controls="messages" aria-selected="false"><b> MESSAGES</b></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" id="documents-tab" data-toggle="tab" href="#documents" role="tab" aria-controls="documents" aria-selected="false"><b>DOCUMENTS</b></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" id="time-line-tab" data-toggle="tab" href="#time-line" role="tab" aria-controls="time-line" aria-selected="false"><b> STORY-LINE</b></a>
            </li>
          </ul>
          <h3 style="color: #1F487E; display:flex; justify-content: flex-end;">
              <%= @case.title.upcase %>
          </h3>
        </div>
        <!-- <div class="fake-column-3"></div> -->
      </div>
      <div class="tab-content " id="myTabContent">
        <div class="tab-pane fade tab-content-text in active" id="overview" role="tabpanel" aria-labelledby="overview-tab">
          <div class="four-columns-dash">
            <div class="left-column-dash">
              <div class="text-center">
                <%= cl_image_tag @case.user.photo, :width=> 300, :height=>300, :crop=>"fill", class: "show-photo", style:"margin-bottom: 10px;" %> <br>
                <p>
                  <%= @case.user.first_name %> <%= @case.user.last_name %>
                </p>

                <div style="display: flex; flex-direction: column;">
                  <%= link_to "Edit your profile", edit_user_path(@case.user), class: "law-btn" %>
                  <%= link_to "My dashboard", mydashboard_path, class: "law-btn" %>
                </div>

              </div>
            </div>
            <div class="right-column-dash">
              <div class="overview-card-container">

                <div class="overview-card">
                  <p>Case's overall cost - last estimate:</p>
                  <p><strong><%= @case.financial_cost %> $</strong>  </p>

                </div>
                <div class="overview-card">
                  <p>Case's overall length - last estimate:</p>
                  <p><strong> <%= @case.overall_length %> days</strong> </p>

                </div>
                <div class="overview-card">
                  <p>Case's odds - last estimate:</p>
                  <p><strong> <%= @case.odds_success %> % </strong></p>

                </div>
              </div>


            </div>

            <!-- lawyer options -->
            <% if @case.user.lawyer == true %>
            <div class="steps-financial-column-dash text-center">

              <%= link_to "Update Case", edit_case_path(@case), class: "law-btn" %>
              <%= link_to "Accept", accept_case_path(@case), method: :patch, class: "law-btn" %>
              <%= link_to "Decline", decline_case_path(@case), method: :patch, class: "law-btn" %>
            </div>
            <% end %>
          </div>
        </div>
        <div class="tab-pane fade tab-content-text " id="messages" role="tabpanel" aria-labelledby="messages-tab">
          <div class="two-columns-dash">
            <div class="left-column-dash">
              <div class="text-center">
                <%= cl_image_tag @case.user.photo, :width=> 300, :height=>300, :crop=>"fill", class: "show-photo", style:"margin-bottom: 10px;" %> <br>
                <p>
                  <%= @case.user.first_name %> <%= @case.user.last_name %>
                </p>
                <div style="display: flex; flex-direction: column;">
                  <%= link_to "Edit your profile", edit_user_path(@case.user), class: "law-btn" %>
                  <%= link_to "My dashboard", mydashboard_path, class: "law-btn" %>
                </div>
              </div>
            </div>

          <div style="width: 100%;">
            <div class="right-column-dash">
              <div class="messages">
                <% @case.messages.order(created_at: :desc).each do |message| %>
                <%= render "messages/message", message: message %>
                <% end %>
              </div>
            </div>
              <%= render "messages/form", form_case: @case, message: @message %>
              <div class="clearfix"></div>
            </div>
          </div>
        </div>
        <div class="tab-pane fade tab-content-text " id="documents" role="tabpanel" aria-labelledby="documents-tab">
          <div class="two-columns-dash">
            <div class="left-column-dash">
              <div class="text-center">
                <%= cl_image_tag @case.user.photo, :width=> 300, :height=>300, :crop=>"fill", class: "show-photo", style:"margin-bottom: 10px;" %> <br>
                <p>
                  <%= @case.user.first_name %> <%= @case.user.last_name %>
                </p>
                <div style="display: flex; flex-direction: column;">
                  <%= link_to "Edit your profile", edit_user_path(@case.user), class: "law-btn" %>
                  <%= link_to "My dashboard", mydashboard_path, class: "law-btn" %>
                </div>
              </div>
            </div>
            <div class="right-column-dash">
              <%= render "shared/documents" %>
            </div>
          </div>
        </div>
        <div class="tab-pane fade tab-content-text " id="time-line" role="tabpanel" aria-labelledby="time-line-tab">
          <div class="two-columns-dash">
            <div class="left-column-dash">
              <div class="text-center">
                <%= cl_image_tag @case.user.photo, :width=> 300, :height=>300, :crop=>"fill", class: "show-photo", style:"margin-bottom: 10px;" %> <br>
                <p>
                  <%= @case.user.first_name %> <%= @case.user.last_name %>
                </p>
                <div style="display: flex; flex-direction: column;">
                  <%= link_to "Edit your profile", edit_user_path(@case.user), class: "law-btn" %>
                  <%= link_to "My dashboard", mydashboard_path, class: "law-btn" %>
                </div>
              </div>
            </div>
            <div class="right-column-dash">
            </div>
          </div>
        </div>
      </div>
    </div>
    <!-- </div> -->
  </div>
