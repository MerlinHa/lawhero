<h1>Case new page</h1>

<div class="container">
  <div class="row">
    <%= simple_form_for([@request, @case]) do |f| %>
      <%= f.input :title %>
      <%= f.input :description %>
      <%= f.button :submit %>
    <% end %>
  </div>
</div>
