<div class="container container-new-payment">
  <div class="col-sm-6 col-sm-offset-3">
    <div class="row">
      <h1 class="h1-headline-blue">Book your lawyer now<%= @order.lawyer_sku %></h1>
      <script src="https://js.stripe.com/v3/"></script>
      <p > It is just a prepayment. </p>
    </div>

    <div class="row prepayment">
      <p style="font-weight:bold;"> Why I need to pay? </p>

      <p >In order to prove the seriousness of your demand. </p>
      <p > Obviously, It will be deducted from your final bill! </p>
    </div>

    <div class="row">
      <form action="/orders/<%= @order.id %>/payments" method="post" id="payment-form">
        <div class="form-row stripe-payment">
          <label for="card-element">
            Credit or debit card
          </label>
            <div id="card-element">
              <!-- A Stripe Element will be inserted here. -->
            </div>
            <!-- Used to display form errors. -->
            <div id="card-errors" role="alert"></div>
        </div>

        <button class="btn btn-primary btn-payment">Submit Payment</button>

      </form>
    </div>
  </div>
</div>
